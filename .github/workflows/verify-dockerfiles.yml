name: Verify Dockerfiles

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  verify-dockerfiles:
    uses: openEDI/oedisi-components/.github/workflows/reusable_verify_dockerfiles.yml@main
    with:
      component-root: "."
