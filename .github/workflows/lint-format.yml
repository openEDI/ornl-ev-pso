name: Lint and Format

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  lint-format:
    uses: openEDI/oedisi-components/.github/workflows/reusable_lint_format.yml@main
    with:
      python-version: "3.10"
